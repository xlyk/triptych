# Triptych

Minimal initial Go skeleton for the Triptych control plane.

Current scope:
- `tt` CLI entrypoint
- `agentd` daemon entrypoint
- `agentserver` server entrypoint
- shared domain types and request validation
